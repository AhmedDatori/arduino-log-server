# Public Static Files

This folder is reserved for files that Vite should copy directly into the built site, such as icons, images, or downloadable assets.

The main application page is `../index.html`, not a file inside this folder.
